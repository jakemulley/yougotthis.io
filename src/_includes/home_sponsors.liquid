<div class="bg-white">
  <div class="max-w-screen-xl mx-auto py-12 px-4 sm:px-6 lg:py-16 lg:px-8">
    <p class="text-center text-base leading-6 font-semibold uppercase text-gray-600 tracking-wider">
      Thank you to organizations who have supported our events
    </p>
    <div class="mt-6 grid grid-cols-3 gap-0.5 sm:grid-cols-4 md:grid-cols-5 lg:grid-cols-9 lg:mt-8">
      {% for sponsor in sponsors %}
        <img src='/assets/img/sponsors/{{sponsor.file}}.png' alt='{{sponsor.name}}' class='lazyload'>
      {% endfor %}
    </div>
  </div>
</div>